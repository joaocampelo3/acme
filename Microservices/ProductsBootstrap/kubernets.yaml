apiVersion: apps/v1
kind: Deployment
metadata:
  name: products-bootstrap-deployment
spec:
  selector:
    matchLabels:
      app: products-bootstrap
  replicas: 1
  template:
    metadata:
      labels:
        app: products-bootstrap
    spec:
      containers:
      - name: products-bootstrap-container
        image: 1151386/products-bootstrap
        imagePullPolicy: Never
        ports:
        - containerPort: 8080
        - containerPort: 5672
        resources:
          limits:
            cpu: 200m
            memory: 512Mi
          requests:
            cpu: 100m
            memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: products-bootstrap-service
spec:
  selector:
    app: products-bootstrap
  ports:
  - name: amqp
    port: 5672
    targetPort: 5672
    nodePort: 30010
  - name: http
    port: 8080
    targetPort: 8080
    nodePort: 30011
  type: NodePort